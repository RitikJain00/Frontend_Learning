const superHeros : string[] = ["spiderman","ironman"];   // delaration

superHeros.push('captain America')

const Rollno : Array<number> = [2,5,5]   // also decalare like this

type User = {
  name: string,
  email: string
}

const allUsers: User[] = []

allUsers.push({name: "Ritik", email: "ritik@email.com"})

const rgb : number[][] = [    // declaration of 2d Arrays
  [253,152,63],
  [264,156,253]
]